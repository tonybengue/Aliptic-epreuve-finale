{% extends 'base.html.twig' %}

{% block title %}CategoryEntity{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>

    <a href="{{ path('category_edit', {'id': category_entity.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
